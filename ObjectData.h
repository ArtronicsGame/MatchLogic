#ifndef _OBJECT_DATA
#define _OBJECT_DATA

#include<bits/stdc++.h>

using namespace std;

class ObjectData{
    public:
        string id;
        string shapeName;

        ObjectData(string id, string shapeName) : id(id), shapeName(shapeName){}
};
#endif
